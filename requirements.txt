# Deep Learning & Models
# Ghi chú: Đã loại bỏ thư viện `torch` do môi trường máy ảo đã cài đặt sẵn PyTorch 2.9.1 và CUDA 12.8.
# Việc giữ `torch` ở đây có thể khiến pip ghi đè hoặc hạ cấp phiên bản PyTorch gây lỗi không nhận GPU.
transformers>=4.30.0
accelerate>=0.21.0
peft>=0.5.0
datasets>=2.14.0

# Data Processing & Metrics
pandas
numpy
scikit-learn

# Vietnamese NLP processing
underthesea
pyvi
py_vncorenlp

# Utilities
tqdm
torchinfo
jupyter
cleanlab

# Experiment Tracking
mlflow
wandb

# Web Application & API
fastapi>=0.115.0
pydantic>=2.0.0
uvicorn>=0.30.0
requests>=2.31.0
httpx>=0.27.0
pytest>=8.0.0
